# Archer Slack

Internal tool. Classroom version of Slack with three channels for three different CS classes. Supports sharing code.

## Development

Change the URL from prod to dev!

For new codespace: `cd backend && npm install`

To run app: `cd backend && node server.js`
